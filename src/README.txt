README
Name: Bo Wu; Ziyue Qiu

To build and run: 
I provide a complete C project. On Eclipse, File->Open Projects from file system, then import the whole folder(project) from directory.
Then do the regular build and run on the my project.

